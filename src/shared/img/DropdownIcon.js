import React from "react";

const DropdownIcon = ({ open }) => (
  <p>
    <p id="nav-icon2" className={open ? "open" : ""}>
      <span />
      <span />
      <span />
      <span />
      <span />
      <span />
    </p>
  </p>
);

export default DropdownIcon;
